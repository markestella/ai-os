{
  "name": "@macky_aletse/ai-os",
  "version": "1.0.5",
  "description": "Token-optimized AI orchestration CLI for coding agents",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "bin": {
    "ai": "./bin/ai.ts"
  },
  "scripts": {
    "dev": "bun run bin/ai.ts",
    "build": "bun build bin/ai.ts --outdir dist --target node",
    "link": "bun link"
  }
}
